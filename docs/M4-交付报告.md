# M4 交付报告

> 模块：M4 内部交流与通知
> 分支：`feat/p2-community`
> 状态：实现与本地验收完成，**未 push / 未开 PR**
> 报告时间：2026-09-18（2026-09-19 补充上游同步与本地预览结果）

---

## 基线与分支

- 起始基线：`4f5fd37`（Merge PR #40 `feat/p2-member-dashboard`）
- 分支：`feat/p2-community`
- 上游关系：`origin` = 组织仓库 fork 工作流；禁止直接 push 组织 `main`

### 2026-09-19：合并上游 `main`

`main` 已前进到 `db3593a`（PR #41 `fix(member): use capability ids as keys in MemberUpcoming`），
本分支合并之（`451ecee`，两个父提交的合并提交）。

**冲突 1 处：`src/components/member/MemberUpcoming.tsx`。** 值得记录，因为两边动的是同一件事：

- PR #41 修的浏览器报错 `Encountered two children with the same key, M4`，
  根因**正是本模块引入的** —— 通知与收藏两项的 `module` 都是 `"M4"`，而当时 key 取了 `module.module`。
- 但 PR #41 同时新增了单测 `tests/unit/m3-member-upcoming.test.ts`，断言
  `buildUpcomingEntries()` 返回 3 条（`notifications` / `favorites` / `ranking`）；
  而本模块已把通知与收藏换成真实摘要，该函数签名不再成立。

解决方式：

1. 保留本模块的真实摘要结构（通知 / 收藏各自用数据行 `id` 当 key，接入位只剩排行一项）；
2. 把「key 取能力标识而非 `module` 值」这条不变量拆成可测的纯函数，
   移到 `src/features/member-dashboard/upcoming-entries.ts`，并据此收窄上游单测；
3. **删除**本模块原先为消掉「`ranking` 未使用」而加的
   `<span className="sr-only">{ranking.module}</span>` —— 它把内部里程碑编号交给读屏软件，
   与上游「不把 `module` 渲染给终端用户」的约定冲突。抽出纯函数后 `ranking` 有真实用处，无需该元素。

> 为什么拆成单独文件而不是留在组件里：本仓库测试用 `tsx --test`，JSX 按经典运行时编译，
> 而 `Icon.tsx` 在模块顶层就求值 JSX；只要测试 import 组件，就会顺着
> `Button` → `Icon` 抛 `React is not defined`。纯逻辑必须放在与 JSX 无关的 `.ts` 模块中才可测。

同时修正了 `tests/integration/m4-community.test.ts` 的 5 处类型错误（`ae8f821`）：
`callRoute` 的处理器签名原写成 `context?` 可选，而动态段路由把 `params` 声明为必填，
参数逆变不成立（TS2345）；`const extras = []` 隐式 `any[]`（TS7034 / TS7005）。
`tsc --noEmit` 由 5 个错误降为 **0**（CI 没有独立类型检查，这一步需手动跑）。


任务书要求的开工前阅读材料（`AGENTS.md`、`docs/design-system.md`、`docs/architecture.md`、
`docs/database.md`、`docs/contracts/*`、`docs/第二期需求分析文档.md`、M0–M3 交付报告）
均已在实现前通读。产品确认项：提及取请求 ID 与正文 `@姓名` 并集；收藏唯一键为
`memberProfileId + repairRecordId`（取消软删除、再收藏恢复同一行）；回复两层拍平；
工作台 `notifications` / `favorites` 换成真实摘要，`ranking` 仍为 M5 占位。

---

## 已实现范围

### 评论与回复

- 维修详情页评论入口：根评论 + 直接回复；对回复再回复时服务端把 `parentCommentId`
  拍平到根，UI 仍在正文前缀 `@原作者`。
- 分页按根评论计，回复随当前页根评论一次带出。
- 正文拒绝空内容、超过 2000 码点、以及 TAB/LF/CR 以外的 C0 控制字符。
- `@` 解析按最长姓名匹配（昵称 / 实名 / 账号展示名），与显式
  `mentionedMemberProfileIds` 并集；未知显式 ID 拒绝，超限拒绝不截断。
- 作者可删自己的评论；删除他人评论需 `comment:delete`。软删除并写审计。

### 通知

- 类型：`MENTIONED`、`REPAIR_COMMENTED`、`REPAIR_APPROVED`、`REPAIR_REJECTED`。
- 不通知自己；同一评论对记录主人若同时被 @，只发 `MENTIONED`。
- 审核通知在审核事务内写入；审核员没有成员档案时仍通知记录主人。
- 标已读幂等；全部已读只更新未读行；删除走软删除且保留状态。

### 收藏与案例标记

- 收藏唯一约束 `(memberProfileId, repairRecordId)`；取消软删除，再次收藏恢复同一行
  并刷新时间。
- 疑难 / 典型案例复用 M2 的 `RepairRecord.isDifficult` / `isTypical` 与
  `PATCH /api/v1/admin/repairs/:id/flags`；成员详情展示徽章，管理员可改标记。

### 工作台与页面

- `GET /api/v1/member/dashboard` 的 `notifications` / `favorites` 为真实摘要；
  `ranking` 仍为 `{ available: false, module: "M5" }`。
- `degraded` 新增 `notifications` | `favorites`；M4 失败只降级对应区块。
- 页面：`/member/notifications`、`/member/favorites`；详情页评论 / 收藏 / 标记。
- 快捷操作增加通知与收藏入口。文案来自 `src/config/community.ts`。

---

## 公共契约变更

### Schema / Migration

新 Migration：`prisma/migrations/20260918120000_p2_m4_community/migration.sql`

- 新增表 `repair_comments`、`comment_mentions`、`repair_favorites`、`notifications`；
- 未修改任何既有表的列语义，M0–M3 数据无损升级；
- 未改写任何已执行的 Migration，未使用 `prisma db push` 或手工 DDL。

### Type / Permission / Error

- 新增：`RepairCommentView`、`NotificationView`、`FavoriteView`、
  `MemberNotificationSummary`、`MemberFavoriteSummary`，常量
  `COMMENT_BODY_MAX_LENGTH`(2000)、`COMMENT_MENTION_LIMIT`(10)、
  `MEMBER_DASHBOARD_NOTIFICATION_LIMIT`(5)、`MEMBER_DASHBOARD_FAVORITE_LIMIT`(5)。
- `MemberDashboard.notifications` / `favorites` 从 `DeferredModule` 换成真实摘要；
  `degraded` 增加 `notifications` | `favorites`。
- 新增权限：`comment:create`、`comment:read`、`comment:delete`、`favorite:manage`、
  `notification:read`。成员无 `comment:delete`。
- 新增错误码：`COMMENT_NOT_FOUND`(404)、`COMMENT_FORBIDDEN`(403)、
  `COMMENT_PARENT_INVALID`(400)、`COMMENT_BODY_INVALID`(400)、
  `MENTION_LIMIT_EXCEEDED`(400)、`FAVORITE_NOT_FOUND`(404)、
  `NOTIFICATION_NOT_FOUND`(404)。

### API

```text
GET    /api/v1/repairs/:id/comments
POST   /api/v1/repairs/:id/comments
DELETE /api/v1/repairs/:id/comments/:commentId
GET    /api/v1/member/favorites
POST   /api/v1/member/favorites
DELETE /api/v1/member/favorites/:repairId
GET    /api/v1/member/notifications
POST   /api/v1/member/notifications/:id/read
POST   /api/v1/member/notifications/read-all
DELETE /api/v1/member/notifications/:id
```

统一信封 `{ success, data, meta }`，成员数据 `Cache-Control: private, no-store`；
写接口 `assertSameOrigin()`。通知列表的 `data` 为 `{ items, unreadCount }`。

### 对 M3 / M5 的影响

- M3 工作台占位字段在本变更中替换为真实摘要；M3 集成测试断言已同步更新。
- M5 排行仍为 `{ available: false, module: "M5" }`，不预设任何数字。

---

## 数据、安全与隐私

- 业务外键一律指向 `member_profiles.id` / `repair_records.id`，QQ / 手机号 / 学号
  不进入 M4 表或列表字段。
- 评论、收藏可见性继承维修记录对象级权限；他人草稿统一 `REPAIR_NOT_FOUND`。
- 通知只能由收件人本人读取、标已读或软删除；他人 ID 一律 `NOTIFICATION_NOT_FOUND`。
- 评论创建 / 删除、收藏增减写 `AuditLog`，与业务行同事务。
- 默认查询排除 `deleted_at IS NOT NULL`。

---

## 测试结果

全部命令按任务书与 `docs/git-workflow.md` 执行；成功、按设计跳过与环境阻断分别如实记录。

| 命令 | 结果 | 说明 |
|---|---|---|
| `pnpm db:generate` | ✅ exit 0 | Prisma Client 7.10.0 生成成功（本会话先前已跑过） |
| `pnpm db:migrate:deploy` | ⛔ 未执行 | 本机无 `.env` / `DATABASE_URL`，无法连接测试库 |
| `pnpm db:health` | ⛔ 未执行 | 同上；本机仅有 MySQL 8.0.46 Community（端口 3306），即使连上也会被产品校验拒绝 |
| `pnpm test` | ✅ 63 passed / 41 skipped / 104 total | 默认只执行单元与 Contract；41 项真实数据库用例按设计跳过（含 8 项新 M4 集成） |
| `pnpm test:unit` | ✅ 63/63 | 含 8 项新 M4 单元测试与 Migration SQL 契约 |
| `pnpm test:db` | ⛔ 未执行 | 无 `.env`，且本机没有 GreatSQL 8.0.32-27；Docker Desktop 未安装 |
| `pnpm lint` | ✅ exit 0 | 0 error 0 warning（含主题调色板检查） |
| `pnpm build` | ✅ exit 0 | 安装 mdBook v0.5.4 后完整构建通过；`Generating static pages (31/31)`；M4 新增 2 个页面 + 8 个 API 路由进入构建产物 |

> `pnpm build` 首次失败是因为本机没有 mdBook。已用 `winget` 安装固定版 `Rustlang.mdBook 0.5.4`，再用 `MDBOOK_BIN` 指向该二进制后完整构建通过（含浅克隆文档仓库与 `public/handbook/`）。`pnpm-lock.yaml` 曾被本机 pnpm 9 改写成二进制差异，已还原，**不随本 PR 提交**。

### 单元测试（63 项）

M4 新增 `tests/unit/m4-community.test.ts`（8 项），并扩展 `permissions.test.ts`、`migration-contract.test.ts`：

- M4 稳定错误码入册，状态码映射正确（404 / 403 / 400）；
- 5 项新权限已定义：成员可 `comment:create|read`、`favorite:manage`、`notification:read`，无 `comment:delete`；
- 限额常量与任务书一致（正文 2000 码点、提及 10 人、工作台摘要各 5 条）；
- 正文拒绝空内容、超长、NUL / BEL 等非法 C0 控制字符，允许 TAB / LF / CR；
- `@` 按最长姓名匹配，未知 token 静默跳过；显式未知 ID 拒绝；超限拒绝不截断；自己不会被写入提及；
- 评论 / 收藏请求拒绝未知字段；通知 `status` 白名单；
- 展示名按昵称 / 实名 / 账号名回退；摘要截断按码点计；
- 文案不伪造排行或红点；
- M4 Migration 四张表、utf8mb4 / InnoDB、唯一键与成员档案外键；扫描 DDL 时忽略 `--` 注释，避免「手机号」出现在说明文字里误报。

### Contract / 集成测试（8 项，真实数据库，本机未跑）

`tests/integration/m4-community.test.ts` 已写好，独立 UUID 段 `c4000000-…`，清理按 `realName` 前缀 `"M4 "` 限定，与 M0/M2/M3 共享库兼容 `--test-concurrency=1`。覆盖：

- 回复超过两层时拍平到根评论，列表按根评论分页并带出回复；
- `@` 与显式 ID 并集通知；主人同时被 @ 只发 `MENTIONED`；不通知自己；
- 未知显式 ID → `VALIDATION_FAILED`；11 人超限 → `MENTION_LIMIT_EXCEEDED`；
- 标已读幂等（`readAt` 不变）、全部已读、软删除保留 `READ` 状态；
- 收藏取消软删除、再收藏恢复同一行并刷新时间；工作台 `favorites.count === 1`；
- 作者可删自己的评论；其他成员 → `FORBIDDEN`；管理员可删；
- 他人草稿评论 / 收藏 → `REPAIR_NOT_FOUND`；他人通知 / 收藏 → `NOTIFICATION_NOT_FOUND` / `FAVORITE_NOT_FOUND`；
- HTTP：评论 POST 201 + `private, no-store`；通知列表 `data` 为 `{ items, unreadCount }`；收藏 POST 201；未登录 GET 通知 401 且无 `data`。

M3 工作台测试已同步：`createApprovedRepair` 现在会写 `REPAIR_APPROVED` 通知，`unreadCount` 断言按两次通过计为 2。

---

## 已知问题与后续依赖

### 阻断项（需补做）

1. **`pnpm test:db` 本机未跑。** 仓库无 `.env`（已 gitignore），本机没有 GreatSQL 8.0.32-27，Docker 未安装。监听 3306 的是 MySQL 8.0.46 Community，root 无密码被拒绝。**未尝试猜测凭据。** 需在有 `DATABASE_URL` 的 GreatSQL 环境执行 `pnpm db:migrate:deploy` 后 `pnpm test:db`。
2. **完整浏览器状态矩阵未做。** 未用 CDP / 截图覆盖评论回复、通知已读、收藏切换、越权 404 页等交互。
3. **`pnpm db:health` 本机无法正向验证。** 即使连上本地 MySQL，产品校验也会正确拒绝（非 GreatSQL 8.0.32-27）。

### 留给 M5

- 维修排行与统计的正式算法；
- 工作台 `ranking` 仍为 `{ available: false, module: "M5" }`，本模块不预设任何数字。

### 留给 M6

- 案例标记目前复用 M2 管理员 flags API，完整后台界面仍归 M6；
- 评论管理列表、批量处理等运营界面未做。

### 遗留治理

- 本机测试数据库历史上是独立 MySQL 实例，**严格意义上未在 GreatSQL 上验证 M4**。建议 CI 或有 Docker 的环境用 `greatsql/greatsql:8.0.32-27` 复跑一次 `test:db`。
- `COMMENT_FORBIDDEN` 已入错误码表，当前越权删除他人评论走通用 `FORBIDDEN`（成员缺 `comment:delete`）。语义正确，如需更细错误码可在复审时收窄。
- 本机 pnpm 为 9.15.4，项目 `packageManager` 为 12.4.1，且 `pnpm-workspace.yaml` 被 9.x 误读；本会话命令一律 `pnpm --ignore-workspace …`。CI 使用 pnpm 12，不受影响。

---

## 变更清单

### 新增（后端）

```text
prisma/migrations/20260918120000_p2_m4_community/migration.sql
src/features/community/mention.ts
src/features/community/community-http.ts
src/features/community/community-view.ts
src/features/community/comment-service.ts
src/features/community/favorite-service.ts
src/features/community/notification-service.ts
src/app/api/v1/repairs/[id]/comments/route.ts
src/app/api/v1/repairs/[id]/comments/[commentId]/route.ts
src/app/api/v1/member/favorites/route.ts
src/app/api/v1/member/favorites/[repairId]/route.ts
src/app/api/v1/member/notifications/route.ts
src/app/api/v1/member/notifications/read-all/route.ts
src/app/api/v1/member/notifications/[id]/read/route.ts
src/app/api/v1/member/notifications/[id]/route.ts
```

### 新增（前端）

```text
src/config/community.ts
src/components/community/RepairComments.tsx
src/components/community/FavoriteToggle.tsx
src/components/community/FavoriteList.tsx
src/components/community/NotificationInbox.tsx
src/components/community/RepairFlagControls.tsx
src/app/member/notifications/page.tsx
src/app/member/favorites/page.tsx
```

### 新增（测试 / 文档）

```text
tests/unit/m4-community.test.ts
tests/integration/m4-community.test.ts
docs/M4-交付报告.md
```

### 修改

```text
prisma/schema.prisma                                 # RepairComment / CommentMention / RepairFavorite / Notification
src/types/contracts.ts                               # M4 视图、限额、工作台摘要替换 DeferredModule
src/lib/api/errors.ts                                # M4 错误码
src/lib/auth/permissions.ts                          # 5 项新权限
src/features/member-dashboard/member-dashboard-service.ts  # 真实通知/收藏摘要 + 局部降级
src/features/repairs/repair-review-service.ts        # 审核事务内写 REPAIR_APPROVED / REJECTED 通知
src/features/repairs/repair-query-service.ts         # 详情带 isFavorited / canFlag
src/features/repairs/repair-view.ts                  # RepairDetailView 扩展
src/components/repairs/RepairDetail.tsx              # 评论、收藏、案例标记入口
src/components/member/MemberUpcoming.tsx             # 工作台通知/收藏真实摘要
src/components/member/MemberShortcuts.tsx            # 通知与收藏入口
src/components/member/MemberDashboard.tsx            # 透传 M4 区块
src/config/member.ts                                 # 快捷入口与后续接入位文案
src/config/repairs.ts                                # 案例标记文案
src/app/globals.css                                  # .community-* 组件层（无 box-shadow，圆角 2/4/999）
src/app/member/repairs/[id]/page.tsx                 # 详情页接入
tests/integration/m0-database.test.ts                # 清理 M4 子表，避免跨文件互删
tests/integration/m3-member-dashboard.test.ts        # 工作台摘要断言改为真实数据
tests/unit/migration-contract.test.ts                # M4 SQL 契约
tests/unit/permissions.test.ts                       # M4 角色权限
docs/contracts/api-v1.md
docs/contracts/data-contract.md
docs/database.md
docs/architecture.md
docs/design-system.md
docs/phase2-development.md
```

---

## 2026-09-19 补充：本地预览与验收结果

### 本地命令结果（均为合并后的树）

| 命令 | 结果 | 说明 |
|---|---|---|
| `eslint .` | ✅ 0 error 0 warning | |
| `check-theme-palette.mjs` | ✅ 通过 | 16 令牌 × 2 套主题与文档站一致 |
| `tsc --noEmit` | ✅ **0 错误** | 合并前有 5 个（已修，见上文） |
| `tsx --test tests/unit/*.test.ts` | ✅ **65 passed / 0 failed** | 合并前 63 通过、1 失败（上游单测被本模块改动打破，已收窄） |
| `docs:build` + `next build` | ✅ 通过 | 31 个页面；M4 的 2 个页面与 8 个 API 路由均在产物中 |
| `pnpm test:db` | ⛔ 未执行 | 仍无 `.env` / GreatSQL，见下文阻断项 |

### 生产构建预览（`next start`，`http://localhost:3000`）

| 路由 | 状态 |
|---|---|
| `/` `/about` `/join` `/docs` `/login` `/register/member` `/handbook/index.html` | **200** |
| `/member` `/member/notifications` `/member/favorites` `/member/repairs` | **307 → `/login`** |

用无头 Chrome（**全新 profile**）核过：公开页与 member 页**控制台零输出、无失败请求**。

- 主题切换动效回归通过：`--d-theme: .56s`、19 个令牌在 transition 列表、`theme-shift` 类正常加/摘、
  读到 oklab 中间值、`transitionRan: true`。
- **M4 接口未登录行为（不依赖数据库）**：`GET /api/v1/member/notifications`、
  `POST /api/v1/member/notifications/read-all`、`GET /api/v1/member/favorites`、
  `GET /api/v1/repairs/:id/comments` 全部返回 `401 UNAUTHENTICATED`，信封完整、**不含 `data`**、
  带 requestId —— 与源码契约一致。

### 仍未完成的验收（阻断项更新）

1. **已登录态的浏览器交互矩阵仍未做**（评论回复、通知已读、收藏切换、删除评论、越权 404/403）。
   原因：本机**没有 Docker 且当前不是管理员**，WSL 无发行版，无法起 `greatsql/greatsql:8.0.32-27`；
   用户已明确不使用本机那个 MySQL 8.0.46 实例。需先具备可用的 GreatSQL（Docker 或其它环境）。
2. **`pnpm test:db` 同上未执行**（8 项 M4 集成用例仍按设计跳过）。
3. **已登录态截图未生成**。`shots/` 下 14 张未跟踪截图仍是 2026-09-18 的未登录态版本，
   待数据库就绪后与已登录态一起重跑再提交。

### 变更清单补充

新增（前端 / 领域逻辑）：

```text
src/features/member-dashboard/upcoming-entries.ts   # 接入位条目纯函数 + key 不变量（可测）
```


# M5 交付报告

## 基线与分支

- 基线 Commit：`3148d9b3`（`upstream/main`，已含 M4「内部交流与通知」及其后续修复与页脚备案号）
- 开发分支：`feat/p2-analytics-ranking`
- 最终 Commit：以本报告所在 PR 的 head commit 为准
- 是否包含 M4：**是**

## 已实现范围

### 正式统计

- 唯一 Analytics 入口 `src/features/analytics/`（Repository / Service / Policy / View / HTTP）
- 个人累计维修数、本月维修数、本学期维修数、累计维修时长，来源标注 `M5_ANALYTICS`
- 故障分类分布（数据库内按 `categoryId` 聚合；无分类历史记录归入稳定的「未分类」桶）
- 最近 12 个 `Asia/Shanghai` 自然月趋势（数据库按月聚合，Service 补齐缺失月份为真实 `0`）
- 无数据 / 学期未配置 / 区块失败三种状态各自明确，互不冒充

### 排行榜

- 本月榜、本学期榜、总榜；默认按维修数量，可切换维修时长
- 竞赛并列语义：主指标相同则同名次、后续跳号（`10,10,8 → 1,1,3`，由 GreatSQL `RANK()` 计算）
- 分页 `pageSize` 1–100，默认 20；「我的排名」独立于当前分页
- 零记录成员不入榜；`REVOKED` 或软删除的成员不进入当前榜单（历史数据保留）
- 工作台预览与完整榜单**共用同一套排名实现**，不写第二套逻辑

### 页面

- `/member/rankings`：范围切换、指标切换、完整分页列表、当前成员标识、空/未配置/加载/局部错误与重试
- `/member`：M3 的排行占位替换为本学期数量榜 Top 3 + 我的排名 + 进入完整榜单的链接
- `/member/profile`：接入分类分布与 12 个月趋势
- 图表**不引入任何图表库**：分类分布与趋势都用表格呈现，趋势额外叠加 `aria-hidden` 的
  装饰性比例条 —— 语义全部在文字里，不存在「颜色是唯一线索」

## 公共契约变更

### Enum / Type（`src/types/contracts.ts`）

- 新增枚举：`AnalyticsScope`(MONTH/TERM/ALL_TIME)、`RankingMetric`(REPAIR_COUNT/DURATION_MINUTES)、
  `AnalyticsStatus`(AVAILABLE/UNCONFIGURED)
- 新增类型：`AnalyticsRange`、`MemberAnalyticsSummary`、`CategoryDistributionItem`、
  `MonthlyTrendPoint`、`MemberAnalytics`、`RankingEntry`、`RankingResult`、
  `MemberRankingPreview`、`RankingQueryInput`
- 新增常量：`RANKING_PAGE_SIZE_DEFAULT`(20)、`RANKING_PAGE_SIZE_MAX`(100)、
  `RANKING_PREVIEW_LIMIT`(3)、`ANALYTICS_TREND_MONTHS`(12)、`UNCATEGORIZED_LABEL`
- 新增服务契约：`MemberAnalyticsServiceContract`、`RankingServiceContract`
- `MemberRepairSummary.source` 从单一字面量扩为 `"M2_APPROVED_REPAIRS" | "M5_ANALYTICS"`
- `MemberDashboard.ranking` 由 `DeferredModule` 替换为 `MemberRankingPreview`
- `MemberDashboardDegraded` 增加 `"ranking"`

> **对任务书冻结形状的一处有意偏离**：任务书把 `MemberRankingPreview` 写成
> `available: true` 的固定形状。若失败时也返回 `available: true` + 空 `leaders`，
> 接口层面就分不出「查询失败」与「本学期真的无人上榜」。因此本实现把它做成三态联合：
> `available: true & status: AVAILABLE`（正常）/ `available: true & status: UNCONFIGURED`（学期未配置）
> / `available: false & status: null`（加载失败）。字段含义未变，只是把失败态显式化，
> 与 M4 已确立的 `MemberNotificationSummary` 失败语义保持一致。

### Permission

- 新增 `analytics:read_internal`，授予 `MEMBER` 与 `ADMIN`；未授予游客或其他身份

### Error

- 新增 `ANALYTICS_SCOPE_INVALID`(400)、`RANKING_METRIC_INVALID`(400)
- 其余复用既有错误码（401/403/404/500 语义未变）

### API

```text
GET /api/v1/member/analytics                       # 个人统计 + 分类分布 + 12 个月趋势
GET /api/v1/member/rankings?scope&metric&page&pageSize   # 内部排行榜
GET /api/v1/member/dashboard                       # ranking 由占位改为真实预览
```

统一信封与 `requestId` 不变；两者均 `Cache-Control: private, no-store`。
排行榜的 `pagination` 只放在 `data` 内（契约字段），**不在 `meta` 里重复一份**，避免两处状态漂移。

### 对 M3 / M4 / M6 / M7 的影响

- **M3**：`repairSummary` 改由 M5 Analytics 入口产出（仍复用 M2 的正式谓词与同一套聚合），
  `ranking` 占位替换；M3 集成测试中两处旧占位断言与一处来源断言同步更新
- **M4**：`notifications` / `favorites` 契约与实现**未改动**
- **M6**：排行名次不写回 `MemberProfile`；成员管理与统计配置后台均未实现
- **M7**：未实现任何公开统计或公开排行榜。本阶段的展示名规则**不是**公开隐私策略，
  M7 上线前必须另行实现管理员可配置的公开策略与公开 DTO

## 数据库与性能

本节结果来自 Docker 中的真实 **GreatSQL 8.0.32-27**（镜像
`greatsql/greatsql:8.0.32-27`），不是 MySQL 替代实例。`db:health` 实测返回：

```text
product=GreatSQL
version=8.0.32-27
versionComment=GreatSQL, Release 27, Revision aa66a385910
timeZone=+00:00
characterSet=utf8mb4
collation=utf8mb4_unicode_ci
```

### Migration

**M5 未新增任何 Migration，也未新增索引。**

### 新增索引及理由

无。四类正式查询在目标 GreatSQL 上均已命中既有索引，未出现 `repair_records` 的全表扫描，
按任务书「只有证明确有必要时才新增」的要求，不添加重叠索引。

### EXPLAIN 摘要

测试数据量：总记录 **3605** 条（其中本次注入 3600 条）、ACTIVE 成员 **116** 名；
注入方式为 60 名成员 × 12 个月 × 5 条，跑完即清理。

| 查询 | 关键计划片段 | 结论 |
|---|---|---|
| ① 个人统计 | `Index lookup on rr using repair_records_member_status_date_idx (member_profile_id=…, status='APPROVED')` | 命中现有复合索引 |
| ② 分类分布 | 同上索引 + `Aggregate using temporary table` | 组内行数仅该成员自身记录，临时表代价可接受 |
| ③ 月度趋势 | `Index range scan on rr using repair_records_member_status_date_idx over (… AND '2025-10-01' <= repair_date < '2026-10-01')` | 日期窗口下推为索引范围扫描 |
| ④ 分组排行 | `Index lookup on rr using repair_records_status_submitted_idx (status='APPROVED')` + `Single-row index lookup on mp using PRIMARY` + `Window aggregate: rank()` | 维修表按正式状态走索引，成员档案走主键回表并过滤 ACTIVE/未删除；聚合/窗口的临时表与排序是 `GROUP BY` + `RANK()` 的固有代价 |

`repair_records` 上唯一的既有索引 `repair_records_member_status_date_idx`
（`member_profile_id, status, repair_date`）正好覆盖成员维度 + 状态 + 日期窗口，
因此没有新增索引的证据。

### 锁表与回退说明

无 DDL，不涉及锁表；无回退需求。

## 统计口径核对

- **APPROVED + deletedAt IS NULL**：唯一谓词来源仍为 M2 的 `approvedRepairWhere()`。
  原生 SQL（窗口函数与 `DATE_FORMAT` 无法用 Prisma 查询构造器表达）里的谓词由
  `approvedRepairSql()` **从同一入口机械翻译**，并对「状态是字面量、删除条件是 `IS NULL`」
  做运行时断言 —— 一旦有人改动 `approvedRepairWhere()`，这里会立刻抛错而不是静默沿用旧口径。
- **日期**：复用 M3 的 `resolveMemberRanges()`（内部即 `shanghaiMonthRange` 与学期配置），
  三个入口（工作台 / 个人主页 / 排行榜）共用同一套 `Asia/Shanghai` 规则。
  实测确认 `repair_date` 是 `DATE` 列，M3 的 UTC 半开区间在它上面的比较语义
  **恰好等价于上海自然月首末两天**（`8/31 → 排除`、`9/1 → 包含`、`9/30 → 包含`、`10/1 → 排除`），
  因此月度分桶直接用 `DATE_FORMAT(repair_date, '%Y-%m')`，**不加时区偏移**。
- **数量 / 时长**：数量取有效记录数；时长取 `SUM(duration_minutes)`，单位恒为分钟，
  `NULL` 按时长 0 计入且不让榜单失败；小时只在 UI 层用既有 `formatDurationMinutes` 格式化，
  排序值不受影响，不使用浮点小时。
- **并列与分页**：名次由 GreatSQL `RANK()` 在**全体有效成员**的聚合集合上计算；
  分页用 `LIMIT/OFFSET` 下推数据库；「我的排名」复用同一排名子查询后在外层按成员过滤，
  因此与榜单里那一行永远一致且不受分页影响。
- **分类 / 趋势与摘要核对**：集成测试断言
  `Σ categoryDistribution.approvedCount === summary.totalApprovedCount`、
  趋势合计与摘要一致、趋势长度恒为 12 且最后一个月是当前上海自然月。

## 权限与隐私

- **未授权测试**：未登录访问两个接口均返回 **401**；缺少 `analytics:read_internal` 的 actor
  调用 Service 抛 `FORBIDDEN`(403)；非法 `scope`/`metric`/分页参数返回稳定 400
- **越权防护**：统计与排行榜都**只接受由 Session 推导出的本人身份**，不接受客户端传入
  `memberProfileId`，因此无法通过改参数遍历他人统计
- **返回字段检查**：契约层有 `RankingEntry` 字段白名单测试（禁止 QQ/手机号/学号/班级/`userId`/昵称）；
  集成测试对真实响应做正则断言；运行时冒烟同样确认无身份字段
- **日志与缓存检查**：两个接口均 `Cache-Control: private, no-store`；
  含 `isCurrentMember` 的响应绝不进入共享缓存；统计读取**不写业务审计日志**（避免放大 `AuditLog`）；
  性能日志只记录 scope / metric / pageSize / 耗时 / requestId

## 测试结果

| 命令 | 退出码 | 用例数 | 说明 |
|---|---|---|---|
| `db:generate` | 0 | — | Prisma Client 7.10.0 |
| `db:migrate:deploy` | 0 | — | 5 migrations found，No pending |
| `db:health` | 0 | — | 真实 GreatSQL 8.0.32-27；产品、版本、UTC、utf8mb4 与排序规则全部通过 |
| `test:unit` | 0 | **98 / 98** | 新增 `m5-analytics.test.ts`(26) 与 `m5-contracts.test.ts`(8)；删除已废弃的 `m3-member-upcoming.test.ts`(2) |
| `test:db` | 0 | **62 / 62** | 真实 GreatSQL 8.0.32-27；新增 `m5-analytics-ranking.test.ts`(20)，M0–M4 基线 42 项全绿 |
| `test`（`pnpm test`） | 0 | 160 total / 98 pass / **62 skipped** | 集成测试按设计被门控跳过 |
| `lint`（`eslint .` + 调色板） | 0 | 0 problems | `.codex-tmp/**` 已加入 eslint ignores，标准 lint 现在真正通过 |
| `eslint src tests tools prisma` | 0 | — | 源码目录干净 |
| `tsc --noEmit` | 0 | — | — |
| `build` | 0 | 32 / 32 静态页；68 路由 | 完整 `docs:build + next build`；M5 三个路由均进入产物 |
| Desktop / Mobile × Normal / Dark | 通过 | 12 个常规组合 | 三个页面四象限均完成，无横向溢出；另完成空/未配置/错误/重试态 |

### 新增测试覆盖要点

- **单元**（`m5-analytics.test.ts`）：MONTH/TERM/ALL_TIME 解析、学期未配置、
  上海自然月与跨年边界、12 个月补零、`NULL` 时长、BigInt/字符串/null/NaN 数值转换与
  JSON 可序列化、分类分布排序与「未分类」桶、展示名回退链、入榜条件、
  scope/metric 白名单与稳定错误码、失败占位语义、`approvedRepairSql` 取值来自唯一谓词、
  **正式谓词键集合被钉死**、**竞态守卫的「只允许最新请求生效」语义**
- **契约/权限**（`m5-contracts.test.ts`）：枚举无重复、错误码映射 400、
  `analytics:read_internal` 仅授予 MEMBER/ADMIN、边界常量、`RankingEntry` 字段白名单
- **集成**（`m5-analytics-ranking.test.ts`，20 项）：非 APPROVED 与软删除不入统计、
  月/学期边界、学期未配置不伪造空榜、数量榜并列 `1/1/3` 与稳定次序、时长榜排序、
  `NULL` 时长、分页与页外「我的排名」、零记录成员、`REVOKED`/软删除成员、
  分类分布与趋势合计核对、软删除后统计同步、工作台接线、401/403（含**预览路径**）/400、
  隐私与信封、**个人主页与内部主页摘要来源统一为 `M5_ANALYTICS`**

## 审核意见修复（第二轮）

| # | 优先级 | 意见 | 处理 |
|---|---|---|---|
| 1 | P1 | 未在目标 GreatSQL 上验证 | ✅ 已修：Docker 运行 `greatsql/greatsql:8.0.32-27`，Migration、健康检查、62 项集成测试和四类 EXPLAIN 全部复跑通过 |
| 2 | P1 | 个人主页摘要未接入 M5 | ✅ 已修：`getSelf` 与 `getInternal` 改走 `getMemberSummary()`，`source` 统一为 `M5_ANALYTICS`；新增集成测试锁定三个入口来源与数字一致 |
| 3 | P1 | DoD 未满足（lint / 浏览器验收） | ✅ 已修：`.codex-tmp/**` 加入 eslint ignores 与 `.gitignore`；标准 lint 通过，浏览器四象限及空/未配置/错误/重试态均完成 |
| 4 | P2 | 排行预览绕过 Analytics 权限 | ✅ 已修：`getTermPreview(memberProfileId, actor)` 增加 actor 参数并校验 `analytics:read_internal`，契约注释说明若不校验即为读任意成员名次的旁路；集成测试补充该路径的 403 |
| 5 | P2 | 排行筛选存在旧响应覆盖新结果的竞态 | ✅ 已修：抽出可单测的 `createLatestOnlyGuard()`（`src/features/analytics/latest-only.ts`），`RankingsPageBody` 只允许最新请求写入状态；单元测试覆盖「旧 token 失效」与「实例间互不干扰」 |
| 6 | P2 | 谓词漂移断言不完整 | ✅ 已修：`approvedRepairSql()` 改为断言**键集合完全一致**（`deletedAt,status`），新增第三个正式条件会当场抛错；单元测试同时钉死该假设 |
| — | 清理 | `pnpm-lock.yaml` 带入本地包管理器状态 | ✅ 已 `git restore` 还原为提交状态。（注：`@pnpm/exe` 条目**基线中本就存在**，本地漂移的是另外约 853 字节的平台包条目） |

### 关于第 6 条的实现选择

评审建议「断言键集合完全一致，或把 Prisma/SQL 谓词共同建立在同一份正式口径常量上」。
本次采用**前者**：改动最小、不触碰 M2 已冻结的 `approvedRepairWhere()` 签名与语义
（后者需要重构跨模块的公共契约，超出 M5 范围且会影响 M2/M3 的既有测试）。
代价是维护者改谓词时会遇到一次显式失败并需要同步 SQL —— 这正是期望的行为，
因为它把「静默口径分叉」变成了「必须处理的中断」。

## 基线测试修复（独立于 M5 功能）

### 1. 本地测试运行方式与 `package.json` 不一致（**问题根源**）

排查中发现 `package.json` 的 `test:db` **本来就带 `--test-concurrency=1`**，
而本地包装脚本 `.workbuddy/run.mjs` 漏抄了该参数，导致集成测试以**并行模式**运行。
Node 测试运行器默认按文件并行起进程，而所有集成测试**共用同一个 GreatSQL 库** ——
进程互相插数据，表现为外键冲突、写冲突/死锁、以及 42 项里随机 14~15 项失败。

补上该参数后 **42/42 全绿**（代码未改动）。已修正 `run.mjs` 的 `test:db` / `test:all` 与
`package.json` 对齐。

> 这条不是产品缺陷，而是「本地工具链与项目脚本不一致」——记录在此以免后人重复排查。

### 2. `m0-database.test.ts` 清理序列对残留状态不成立

即便串行执行，该文件的「全表清空 + 重建 fixture」对某些残留状态仍会失败。
已按最小、确定性的方式修复，**未弱化生产外键、未改历史 Migration、未在业务代码规避、
未使用 `FOREIGN_KEY_CHECKS=0`**：

- **`repair_comments.parent_comment_id` 是自引用外键**（回复 → 根评论，`ON DELETE RESTRICT`）：
  一条 `deleteMany()` 会在删根评论时被尚未删掉的回复挡住 →
  改为**两步删**（先删 `parentCommentId != null`，再删 `= null`）。
  `m3-member-dashboard.test.ts` 的 `prepareFixtures` 同样处理。
- **`skills.created_by` → `users`（可空、`ON DELETE RESTRICT`），而 `skills` 是刻意保留的
  共享 seed 数据**：只要任意一行技能带着创建者（测试、预览脚本或历史运行留下），
  全表清空 `users` 就会被外键挡住 → 删 `users` 前先
  `skill.updateMany({ where: { createdBy: { not: null } }, data: { createdBy: null } })`，
  **只解绑引用、不删除共享 seed 技能**（与既有保留 `roles` 的策略一致）。

排查方式（可复用）：查 `information_schema.KEY_COLUMN_USAGE` 枚举全部外键，
标注「引用列是否可空」与「是否在清理集合内」，再对清理集合做拓扑排序 ——
唯一落在集合外的引用表就是 `skills`。

## 页面验收（真实浏览器 + 本地测试数据）

使用本地 GreatSQL 测试库和测试成员会话，对三个页面完成真实浏览器验收；未连接生产环境，
未访问或修改 `47.116.132.37`。

| 检查 | 结果 |
|---|---|
| `/member`、`/member/profile`、`/member/rankings` | Desktop 1440×900 / Mobile 390×844 × Normal / Dark 全部通过；无横向溢出 |
| 加载态 | 三个页面均实际观察到加载提示，完成后正确替换为数据或空态 |
| 排行筛选与分页 | MONTH / TERM / ALL_TIME、数量 / 时长均可切换；25 人测试榜分页 1→2 正常；我的排名不受分页影响 |
| 数据与空态 | 工作台显示累计 18 次及 Top 3；个人页分类/12 月趋势一致；软删除本地测试记录后排行榜与分类正确显示空态 |
| 学期未配置 | 移除本地学期环境变量并重启预览后，TERM 显示「本学期区间未配置」，不伪造空榜 |
| 错误与重试 | 页面保持已登录时停止本地 GreatSQL，再切换范围，显示「排行榜加载失败」及「重新加载」；恢复数据库后点击重试成功恢复 |
| Console | 常规与恢复后的页面 **0 error**；仅有一条 Next.js 开发模式关于 `data-scroll-behavior="smooth"` 的前瞻 warning |
| `GET /api/v1/member/analytics` | 200、`private, no-store`、`source: M5_ANALYTICS`、趋势恒为 **12** 点、响应无身份字段 |
| 排行榜 6 种 `scope × metric` 组合 | 全部 200；`MONTH`/`ALL_TIME` 为 `AVAILABLE`，`TERM` 未配置时为 `UNCONFIGURED` 且 `items` 为空（**未伪造空榜**） |
| 非法参数 | `scope=YEAR` → 400 `ANALYTICS_SCOPE_INVALID`；`metric=AVG` → 400 `RANKING_METRIC_INVALID`；`pageSize=101` → 400 `VALIDATION_FAILED` |
| 未登录 | 两个接口均 401 |

## 已知问题与后续依赖

### 验收结论

- 任务书要求的真实 GreatSQL 正向验证、四类 EXPLAIN、浏览器四象限与异常状态均已完成，
  当前无 M5 阻断项。
- 浏览器控制台保留一条 Next.js 开发模式前瞻 warning；不影响功能、生产构建或本次验收，
  后续升级 Next.js 时可按官方提示统一处理根布局属性。

### 留给 M6

- 成员管理后台、分类与软删除管理 UI、统计配置后台
- 若要求「公开真名 / 隐藏 / 仅昵称」可配置，需在 M6/M7 定义策略存储

### 留给 M7

- 公开统计与公开排行榜。**必须另行实现**管理员可配置的公开隐私策略与公开 DTO，
  **不得**直接复用内部排行榜响应或本阶段的展示名规则
- 消费汇总结果应经 M5 的受控适配层，而不是读取内部榜单响应后直接公开

### 其他

- M5 未引入 Redis、消息队列、离线计算、快照表或双写计数器；统计实时聚合
- 排榜名次不写回 `MemberProfile`；审核流程不写累计计数（避免退回/软删除导致漂移）
